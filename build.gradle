group 'modis.students'
version '1.0-SNAPSHOT'

apply plugin: 'application'

mainClassName = "modis.students.Main"

repositories {
    mavenCentral()
}

run{
    standardInput = System.in
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.11'
}
